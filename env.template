# Finance App - Environment Variables Template
# 
# This template file contains default values for local development.
# Copy this file to create your environment configuration files.
#
# ============================================================================
# ENVIRONMENT FILE OPTIONS
# ============================================================================
#
# 1. .env.local
#    - For local development with Docker/local PostgreSQL and MailHog
#    - Uses: Local PostgreSQL, MailHog for email testing
#    - Create: cp env.template .env.local
#    - Usage: npm run dev:local
#
# 2. .env.local-prod
#    - For local development with production-like services
#    - Uses: Neon Postgres (cloud), Brevo (production email)
#    - Create: cp env.template .env.local-prod
#    - Then configure with Neon and Brevo credentials
#    - Usage: npx dotenv -e .env.local-prod -- next dev
#    - See: deploy/local-prod/README.md for setup instructions
#
# 3. .env.production
#    - For Vercel deployment (production)
#    - Uses: Production database, Brevo, production URL
#    - Create: Configure in Vercel dashboard or sync from Vercel
#    - NEXT_PUBLIC_APP_URL will be your production URL (e.g., https://your-app.vercel.app)
#    - Usage: Automatically used by Vercel during deployment
#    - See: deploy/vercel/README.md for deployment instructions
#
# ============================================================================
# DATABASE
# ============================================================================
# For .env.local: Use local PostgreSQL (Docker or local install)
# For .env.local-prod: Use Neon Postgres connection string
# For .env.production: Use production database (configured in Vercel)
DATABASE_URL="postgresql://postgres:password@localhost:5432/finance_app"

# ============================================================================
# SECURITY
# ============================================================================
# Generate a secure secret: openssl rand -base64 32
SESSION_SECRET="change-this-to-a-random-32-character-secret"

# ============================================================================
# EMAIL CONFIGURATION
# ============================================================================
# For .env.local: Use MailHog for local email testing
EMAIL_PROVIDER="mailhog"
MAILHOG_HOST="localhost"
MAILHOG_PORT="1025"
NEXT_PUBLIC_MAILHOG_HTTP_URL="http://localhost:8025"

# For .env.local-prod and .env.production: Use Brevo for real email sending
# Uncomment and configure these when using Brevo:
# EMAIL_PROVIDER="brevo"
# BREVO_API_KEY="your-brevo-api-key"
# BREVO_SENDER_EMAIL="noreply@yourdomain.com"
# BREVO_SENDER_NAME="Finance App"

# ============================================================================
# APPLICATION
# ============================================================================
# For .env.local and .env.local-prod: Use localhost
# For .env.production: Will be set to your production URL by Vercel
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_FORCE_GUEST_MODE="false"

# ============================================================================
# QUICK START INSTRUCTIONS
# ============================================================================
#
# LOCAL DEVELOPMENT (.env.local):
#   1. cp env.template .env.local
#   2. Generate SESSION_SECRET: openssl rand -base64 32
#   3. Update DATABASE_URL if using non-default PostgreSQL
#   4. Run: npm run dev:local
#
# LOCAL WITH PRODUCTION SERVICES (.env.local-prod):
#   1. cp env.template .env.local-prod
#   2. Generate SESSION_SECRET: openssl rand -base64 32
#   3. Configure DATABASE_URL with Neon Postgres connection string
#   4. Configure EMAIL_PROVIDER="brevo" and Brevo credentials
#   5. Run: npx dotenv -e .env.local-prod -- next dev
#   6. See deploy/local-prod/README.md for detailed setup
#
# PRODUCTION DEPLOYMENT (.env.production):
#   1. Configure environment variables in Vercel dashboard
#   2. Or sync from Vercel: tsx deploy/vercel/sync-env.ts
#   3. NEXT_PUBLIC_APP_URL will be automatically set to your Vercel URL
#   4. See deploy/vercel/README.md for deployment instructions
#
# ============================================================================
# ADDITIONAL RESOURCES
# ============================================================================
# - Local Development: deploy/local-prod/README.md
# - Docker Setup: deploy/docker/README.md
# - Vercel Deployment: deploy/vercel/README.md

